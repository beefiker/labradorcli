// Helpers in this file referenced `crate::cloud_object` types that were
// removed during the fork-divergence cleanup; the file is intentionally
// left empty until any remaining graphql schema utilities are reintroduced.
